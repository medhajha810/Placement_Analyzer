# Supabase Configuration
# Get these values from your Supabase project settings (Settings > API)

# Public API URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Public Anonymous Key (used by browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service Role Key (KEEP SECRET - used only on server for admin operations)
# Get this from Supabase Dashboard > Settings > API > Service role key
# This is used for auto-confirming emails during development
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Copy this file as .env.local and fill in your actual values
# .env.local should NEVER be committed to git (it's in .gitignore)
